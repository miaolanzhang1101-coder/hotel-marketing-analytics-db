# Copy to .env and fill in. Never commit .env.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: target schema (created if missing; defaults to public)
# DB_SCHEMA=analytics

# Optional overrides
# CONFIG_PATH=config.yaml
# DATA_DIR=data
